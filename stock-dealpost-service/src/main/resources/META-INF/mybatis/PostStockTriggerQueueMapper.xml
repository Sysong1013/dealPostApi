<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.dangdang.stock.dealpost.dao.PostStockTriggerQueueMapper">

	<insert id="insertQueueBatch" parameterType="java.util.List">
		INSERT INTO 
				post_stock_trigger_queue (order_id,product_id,warehouse_id,op_num,op_source_id,cart_post_stock_id
		,cart_post_status,stock_type_id,creation_date,last_changed_date,cart_post_date,effect_post_status)
		VALUES
			<foreach collection="list" item="queue" separator=",">
				(#{queue.orderId},#{queue.productId},#{queue.warehouseId},#{queue.opNum},2,#{queue.cartPostStockId}
				,0,#{queue.stockTypeId},now(),now(),now(),#{queue.effectPostStatus})
			</foreach>
	</insert>
</mapper>
		 
		 

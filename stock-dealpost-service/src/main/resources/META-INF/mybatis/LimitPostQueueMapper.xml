<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.dangdang.stock.dealpost.dao.LimitPostQueueMapper">

	<insert id="insertQueueBatch" parameterType="java.util.List">
		INSERT INTO 
			limit_post_queue (order_id,product_id,warehouse_id,op_num,stock_type_id,order_time,source,source_id,delmark,creation_date)
		VALUES
			<foreach collection="list" item="queue" separator=",">
				(#{queue.orderId},#{queue.productId},#{queue.warehouseId},#{queue.opNum},#{queue.stockTypeId},#{queue.orderTime}
				,#{queue.source},#{queue.sourceIds},0,now())
			</foreach>
	</insert>
</mapper>
		 
		 
